{
  "name": "kohen-mcp",
  "version": "0.1.0",
  "description": "Portable agentic MCP server — uniform vault access for any AI tool",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "start": "bun src/server.ts",
    "build": "bun build src/server.ts --outdir dist --target bun"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@octokit/rest": "^21.1.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
